services:
  web:
    build: .
    # Este comando arranca Django y detecta cambios automáticamente
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    environment:
      - DEBUG=1